server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/old_classmate
    password: p
    username: postgres

  servlet:
    multipart:
      max-file-size: 100MB

    hikari:
      minimum-idle: 5
      idle-timeout: 180000
      maximum-pool-size: 12
      connection-timeout: 30000
  redis:
    host: localhost
    port: 6379
    password: li936339745
    timeout: 1000
    database: 0
    jedis:
      pool:
        max-idle: 8
        min-idle: 2
        max-wait: 100
        max-active: 10

  application:
    name: OLD-CLASSMATE

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8091/eureka/

mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.leoli.old_classmate.dao.DO
  configuration:
    map-underscore-to-camel-case: true

oc:
  config:
    search-type: elasticsearch
  security:
    enrypt: bcrypt
